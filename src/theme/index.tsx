import { COLORS } from './Colors'
import { FONTS, FontSizes, FontWeights } from './Fonts'
import { SIZES, TextSizes } from './Sizes'
import { LineHeights, SPACES } from './Spacing'

export const Theme = {
  COLORS,
  SPACES,
  LineHeights,
  SIZES,
  TextSizes,
  FONTS,
  FontSizes,
  FontWeights,
}
